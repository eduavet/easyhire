const Router = require('express').Router;
const handlers = require('../handlers/handlers');

const router = Router();
module.exports = router;

// router.post('/api/todos', handlers.apiAddToDo);
// router.get('/api/todos', handlers.apiGetToDos);
