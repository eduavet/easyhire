blue-1-color = #418fde;
blue-2-color = #0855a6;
folder-color = #585353;
template-color = #585353;
font-white-color = #FFFFFF;
font-unread-color = #212529;
tooltip-background-color = #485248;
minimized-background-color = #404040;

//Header component /start
.header {
  background : blue-2-color;
}
.logoutbtn {
  color: font-white-color;
};

#settingsbtn {
  background-color: rgba(0, 0, 0, 0);
  border: none;
  padding-top: 8px;
};
//Header component /end

//Toolbar component /start
.snippet {
  opacity : .6;
  font-size: 0.9em;
}
.searchContainer{
  flex-grow: 2;
  form{
    width: 52%;
  }
}
.inner-addon {
  position: relative;
}

.inner-addon .search-icon {
  position: absolute;
  padding: 10px;
  pointer-events: none;
}

.search-icon{
  color: slategrey;
}

.left-addon .search-icon  { left:  0px;}

.left-addon input  { padding-left:  30px; }
.toolbar{
  a{
    color blue-2-color;
  }
  button.dropdown-item:active {
    background-color: blue-1-color;
    border-color: blue-1-color;
  }
}
.nav-item.show{
  .selectbtn, .actionbtn{
    background : blue-1-color
  }
}

//Toolbar component /end

//Refresh component /start
.sync-btn{
  width 100%;
  margin 0px auto;
  background: blue-1-color;
  border-color: blue-1-color;
  color: font-white-color
  &:hover{
    background blue-2-color;
    border-color: blue-2-color;

  }
}
//Refresh component /end

//Sidebar component /start
.folders {
  .folder-actions{
    opacity  0;
    pointer-events: none;
    transition all 0.7s ease;
  }
  a {
    color folder-color;
    &:hover {
      text-decoration none;
    }
  }
  li {
    &:hover {
      .folder-actions{
        pointer-events: auto;
        opacity  1 ;
        cursor: pointer;
      }
    }
  }

}

.folder-badge{
  background folder-color;
  color white
}
.active-folder{
  border-left 7px solid blue-1-color;
  color:blue-1-color;
  a {
    color: blue-1-color;
  }
  .folder-badge{
    background blue-1-color;
    color white
  }
  &:hover{
    color:blue-1-color;
  }
}
.folder-actions{
  padding 3px;
  margin 3px;
}
.folders> .list-group-item{
  cursor: pointer
}
//Sidebar component /end

//Login component /start
.loginBtn {
  box-sizing: border-box;
  position: relative;
  margin: 0.2em;
  padding: 15px 14px 20px 40px;
  text-align: center
  border: none;
  line-height: 40px;
  white-space: nowrap;
  border-radius: 0.2em;
  font-size: 36px;
  color: #FFF;
  &:hover{
    color: white
  }
}
.loginBtn:before {
  content: "";
  box-sizing: border-box;
  position: absolute;
  top: 0;
  left: 0;
  width: 34px;
  height: 100%;
}
.loginBtn&:focus {
  outline: none;
}
.loginBtn&:active {
  box-shadow: inset 0 0 0 32px rgba(0,0,0,0.1);
}
.loginBtn--google {
  background: #4285f4;

}
.loginBtn--google:before {
  border-right: #4285f4 1px solid;
  background: url('https://s3-us-west-2.amazonaws.com/s.cdpn.io/14082/icon_google.png') 6px 6px no-repeat;
  background-position :center
}
.loginBtn--google:hover{
  a{
    color: white!important;
  }
}
.loginBtn--google:focus {
  background: #5B7BD5;
}

.login-background {
  display: flex;
  align-items: center;
  justify-content: center;
  background: url("/src/assets/images/simply_blurred.jpg") no-repeat;
  background-size: cover;
  height: 100vh;
}

//Login component /end

//Emails component start
.emailsTable{
  td{
    a{
      color: font-unread-color;
      &:hover{
        text-decoration: none;
      }
    }
  }
  tr{
    border: solid;
    border-width: 1px 0;
    border-color: LightGray;
  }
}
.isUnread {
  background-color: FloralWhite   ;
}
.bold {
  font-weight: bold;
}
//Emails component end

//Email component start
.emailContentContainer{
  max-height: 700px;
  overflow: auto;
}
.email-border-top {
  margin-top: 30px;
  border-top: 2px solid tooltip-background-color;
  padding-top: 30px;
}
.notes{
  position relative;
  textarea{
    padding-right: 82px;
  }
}
.noteSaveStatus{
  position: absolute;
  top: 3px;
  right: 20px;
  padding: 0px 4px;
  margin-top: 4px;
  opacity: 0;
  color: #5d5858;
  font-weight: bold;
}
.noteSaveStatus.active{
  opacity: 0.8;
}
.clearNote{
  position: absolute;
  top: 3px;
  right: 7px;
  padding: 0px 4px;
  margin-top: 4px;
}
#replyButton{
  margin-right: 10px;
}
.fluidMedia {
  position: relative;
  padding-bottom: 25%; /* proportion value to aspect ratio 16:9 (9 / 16 = 0.5625 or 56.25%) */
  padding-top: 30px;
  height: 0;
  overflow: hidden;
}

.fluidMedia iframe {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}

//Email component end

//Compose component /start
.compose{
  display none;
  position fixed;
  bottom 1%;
  right 0.5%;
  width 36%;
  height 77%;
  .modal-header{
    flex-direction row-reverse;
  }
  .minimized{
    display none;
  }
  .minimize, .maximize{
    i{
      font-size 16px;
      font-weight 200;
    }
  }
}
.compose.show{
  display block;
}
.compose.minimized{
  display block;
  width: 24%;
  height: 8.5%;
  font-size 23px;
  transition all 0.7s ease;
  .modal-content{
    display none;
  }
  .minimized{
    display block;
    height 100%;
    background minimized-background-color;
    line-height 2;
    color font-white-color;
    &:hover{
      cursor pointer;
    }
    .close{
      color: font-white-color;
      &:hover{
        color font-white-color;
        opacity 1;
      }
    }
  }
}
.compose.maximized {
  display block;
  top:0%;
  left: 0%;
  width 100%;
  height 100%;
  background rgba(7,7,7,0.7);
  .modal-content{
    position fixed;
    top:10%;
    left: 15%;
    width 70%;
    height 80%;
  }
}
//Compose component /end

//Settings component /start
.templates {
  .template-actions{
    opacity  0;
    pointer-events: none;
    transition all 0.7s ease;
  }
  a {
    display block;
    width 100%;
    color template-color;
    &:hover {
      text-decoration none;
    }
  }
  li {
    &:hover {
      .template-actions{
        pointer-events: auto;
        opacity  1 ;
        cursor: pointer;
      }
    }
  }

}

.template-badge{
  background folder-color;
  color white
}
.active-template{
  border-left 7px solid blue-1-color;
  color:blue-1-color;
  a {
    color: blue-1-color;
  }
  .template-badge{
    background blue-1-color;
    color white
  }
  &:hover{
    color:blue-1-color;
  }
}
.template-actions{
  padding 3px;
  margin 3px;
}
.templates> .list-group-item {
  cursor: pointer
}
//Settings component /end
//global styles /start
.w-15 {
  width: 15%!important;
}
.w-20{
  width: 20%!important;
}
.w-40 {
  width: 40%!important;
}
.mt-60{
  margin-top: 60px;
}
.bg-light-blue{
  background blue-1-color;
  color font-white-color;
  &:hover{
    background blue-2-color;
    color font-white-color;
  }
}
.bg-dark-blue{
  color font-white-color
  background blue-2-color;
}
.wrap-words{
  word-wrap: break-word;
}
.tooltip-toggle::before {
  position: absolute;
  top: -80px;
  left: -80px;
  background-color: tooltip-background-color;
  border-radius: 5px;
  color: #fff;
  content: attr(data-tooltip);
  padding: 1rem;
  text-transform: none;
  -webkit-transition: all 0.5s ease;
  transition: all 0.5s ease;
  width: 160px;
  white-space: pre-line;

  .tooltip-toggle::after {
    position: absolute;
    top: -12px;
    left: 9px;
    border-left: 5px solid transparent;
    border-right: 5px solid transparent;
    border-top: 5px solid tooltip-background-color;
    content: " ";
    font-size: 0;
    line-height: 0;
    margin-left: -5px;
    width: 0;
    white-space: pre-line;
  }
}
.tooltip-toggle:hover::before,
.tooltip-toggle:hover::after {
  opacity: 1;
  -webkit-transition: all 0.75s ease;
  transition: all 0.75s ease;
}

.tooltip-toggle::before,
.tooltip-toggle::after {
  color: #efefef;
  font-family: monospace;
  font-size: 10px;
  opacity: 0;
  pointer-events: none;
  text-align: center;
}
.loadedContent{
  width 100%;
}
//global styles /end
table.sortable th:not(.sorttable_sorted):not(.sorttable_sorted_reverse):not(.sorttable_nosort):after {
  content: " \25B4\25BE"
}

.paging {
  color: blue-2-color
}
.page-item.active .page-link {
  z-index: 1;
  color: #fff;
  background-color: blue-1-color;
  border-color: blue-1-color;
}

// DateTime
.rdt {
  position: relative;
}
.rdtPicker {
  display: none;
  position: absolute;
  width: 250px;
  padding: 4px;
  margin-top: 1px;
  z-index: 99999 !important;
  background: #fff;
  box-shadow: 0 1px 3px rgba(0,0,0,.1);
  border: 1px solid #f9f9f9;
}
.rdtOpen .rdtPicker {
  display: block;
}
.rdtOpen{
  input {
    placeholder: 'Pick a time'
  }
}
.rdtStatic .rdtPicker {
  box-shadow: none;
  position: static;
}

.rdtPicker .rdtTimeToggle {
  text-align: center;
}

.rdtPicker table {
  width: 100%;
  margin: 0;
}
.rdtPicker td,
.rdtPicker th {
  text-align: center;
  height: 28px;
}
.rdtPicker td {
  cursor: pointer;
}
.rdtPicker td.rdtDay:hover,
.rdtPicker td.rdtHour:hover,
.rdtPicker td.rdtMinute:hover,
.rdtPicker td.rdtSecond:hover,
.rdtPicker .rdtTimeToggle:hover {
  background: #eeeeee;
  cursor: pointer;
}
.rdtPicker td.rdtOld,
.rdtPicker td.rdtNew {
  color: #999999;
}
.rdtPicker td.rdtToday {
  position: relative;
}
.rdtPicker td.rdtToday:before {
  content: '';
  display: inline-block;
  border-left: 7px solid transparent;
  border-bottom: 7px solid #428bca;
  border-top-color: rgba(0, 0, 0, 0.2);
  position: absolute;
  bottom: 4px;
  right: 4px;
}
.rdtPicker td.rdtActive,
.rdtPicker td.rdtActive:hover {
  background-color: #428bca;
  color: #fff;
  text-shadow: 0 -1px 0 rgba(0, 0, 0, 0.25);
}
.rdtPicker td.rdtActive.rdtToday:before {
  border-bottom-color: #fff;
}
.rdtPicker td.rdtDisabled,
.rdtPicker td.rdtDisabled:hover {
  background: none;
  color: #999999;
  cursor: not-allowed;
}

.rdtPicker td span.rdtOld {
  color: #999999;
}
.rdtPicker td span.rdtDisabled,
.rdtPicker td span.rdtDisabled:hover {
  background: none;
  color: #999999;
  cursor: not-allowed;
}
.rdtPicker th {
  border-bottom: 1px solid #f9f9f9;
}
.rdtPicker .dow {
  width: 14.2857%;
  border-bottom: none;
  cursor: default;
}
.rdtPicker th.rdtSwitch {
  width: 100px;
}
.rdtPicker th.rdtNext,
.rdtPicker th.rdtPrev {
  font-size: 21px;
  vertical-align: top;
}

.rdtPrev span,
.rdtNext span {
  display: block;
  -webkit-touch-callout: none; /* iOS Safari */
  -webkit-user-select: none;   /* Chrome/Safari/Opera */
  -khtml-user-select: none;    /* Konqueror */
  -moz-user-select: none;      /* Firefox */
  -ms-user-select: none;       /* Internet Explorer/Edge */
  user-select: none;
}

.rdtPicker th.rdtDisabled,
.rdtPicker th.rdtDisabled:hover {
  background: none;
  color: #999999;
  cursor: not-allowed;
}
.rdtPicker thead tr:first-child th {
  cursor: pointer;
}
.rdtPicker thead tr:first-child th:hover {
  background: #eeeeee;
}

.rdtPicker tfoot {
  border-top: 1px solid #f9f9f9;
}

.rdtPicker button {
  border: none;
  background: none;
  cursor: pointer;
}
.rdtPicker button:hover {
  background-color: #eee;
}

.rdtPicker thead button {
  width: 100%;
  height: 100%;
}

td.rdtMonth,
td.rdtYear {
  height: 50px;
  width: 25%;
  cursor: pointer;
}
td.rdtMonth:hover,
td.rdtYear:hover {
  background: #eee;
}

.rdtCounters {
  display: inline-block;
}

.rdtCounters > div {
  float: left;
}

.rdtCounter {
  height: 100px;
}

.rdtCounter {
  width: 40px;
}

.rdtCounterSeparator {
  line-height: 100px;
}

.rdtCounter .rdtBtn {
  height: 40%;
  line-height: 40px;
  cursor: pointer;
  display: block;

  -webkit-touch-callout: none; /* iOS Safari */
  -webkit-user-select: none;   /* Chrome/Safari/Opera */
  -khtml-user-select: none;    /* Konqueror */
  -moz-user-select: none;      /* Firefox */
  -ms-user-select: none;       /* Internet Explorer/Edge */
  user-select: none;
}
.rdtCounter .rdtBtn:hover {
  background: #eee;
}
.rdtCounter .rdtCount {
  height: 20%;
  font-size: 1.2em;
}

.rdtMilli {
  vertical-align: middle;
  padding-left: 8px;
  width: 48px;
}

.rdtMilli input {
  width: 100%;
  font-size: 1.2em;
  margin-top: 37px;
}

.rdtTime td {
  cursor: default;
}